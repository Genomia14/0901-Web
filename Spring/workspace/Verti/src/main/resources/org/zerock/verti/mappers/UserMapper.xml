<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.verti.mappers.UserMapper">

    <insert id="insert" parameterType="org.zerock.verti.vo.UserVO">
        INSERT INTO verti_user (id, email, password, name, phone)
        VALUES (#{id}, #{email}, #{password}, #{name}, #{phone})
    </insert>

    <select id="selectLogin" parameterType="org.zerock.verti.dto.UserDTO" resultType="org.zerock.verti.vo.UserVO">
        SELECT *
        FROM verti_user
        WHERE id = #{id} AND password = #{password}
    </select>

</mapper>
